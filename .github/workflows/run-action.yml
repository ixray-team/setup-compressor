name: 'Run action'

on:
  push:
    branches:
      - '**'
    paths:
      - '.github/workflows/**'
      - 'src/**'
      - 'dist/**'
      - 'package*.json'
      - 'action.json'

jobs:
  setup-latest:
    name: 'Setup latest'

    strategy:
      matrix:
        system:
          - windows-latest

    runs-on: ${{ matrix.system }}

    steps:
      - name: Setup compressor
        id: setup-compressor
        uses: ixray-team/setup-ixray-compressor@5aff6fa45f29c85e13c36f95dff7c2667f3599a6
        with:
          codebase: '1.6-stcop'
          release: 'latest'

      - name: Print info
        run: |
          xrCompress
